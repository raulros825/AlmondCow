{% comment %} Blank template : This is only for dev theme Foreword-Master 1 {% endcomment %}

<div class="page-width pages-{{ page.id }}">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
